# Minimal configuration for development testing
NODE_ENV=development
PORT=3000
JWT_SECRET=test_jwt_secret_key_for_development
JWT_EXPIRES_IN=24h

# Database Configuration (will fail to connect but won't crash immediately)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=partner_management
DB_USER=postgres
DB_PASSWORD=test_password

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=info
